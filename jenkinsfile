pipeline{
      agent{dockerfile true}
      stages{
          stage('build'){
             steps{
                 sh 'python --version'
             }
          }
          stage('test'){
             steps{
                 sh 'python3 calculator_test.py'
                 sh 'echo "Testing Complete1";exit 0'
                 }
              }
           }
         }        
           
